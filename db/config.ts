import { column, defineDb, defineTable } from "astro:db";

export const Voorraad = defineTable({
	columns: {
		aandrijving: column.text({ optional: true }),
		aantal_assen: column.text({ optional: true }),
		aantal_cilinders: column.text({ optional: true }),
		aantal_deuren: column.text({ optional: true }),
		aantal_eigenaren: column.text({ optional: true }),
		aantal_versnellingen: column.text({ optional: true }),
		aantal_zitplaatsen: column.text({ optional: true }),
		accessoires: column.text({ optional: true }),
		accu_conditie: column.text({ optional: true }),
		accu_laadsnelheid: column.text({ optional: true }),
		accu_laadtijd: column.text({ optional: true }),
		accu_snellaadsnelheid: column.text({ optional: true }),
		accu_snellaadtijd: column.text({ optional: true }),
		actieprijs_bedrag: column.text({ optional: true }),
		actieprijs_bpm: column.text({ optional: true }),
		actieprijs_btw: column.text({ optional: true }),
		actieprijs_btw_percentage: column.text({ optional: true }),
		actieprijs_munteenheid: column.text({ optional: true }),
		afbeeldingen: column.text({ optional: true }),
		apk_bij_aflevering: column.text({ optional: true }),
		apk_tot: column.text({ optional: true }),
		assen_aangedreven: column.text({ optional: true }),
		attachment_passend_op: column.text({ optional: true }),
		basiskleur: column.text({ optional: true }),
		bekleding: column.text({ optional: true }),
		bieden_toestaan: column.text({ optional: true }),
		bijtelling_pct: column.text({ optional: true }),
		bouwjaar: column.text({ optional: true }),
		bovenarmlengte: column.text({ optional: true }),
		bovenarmlengte_eenheid: column.text({ optional: true }),
		bpm_bedrag: column.text({ optional: true }),
		brandstof: column.text({ optional: true }),
		breedte: column.text({ optional: true }),
		btw_marge: column.text({ optional: true }),
		carrosserie: column.text({ optional: true }),
		cilinderinhoud: column.text({ optional: true }),
		co2_uitstoot: column.text({ optional: true }),
		datum_binnenkomst: column.text({ optional: true }),
		datum_deel_1: column.text({ optional: true }),
		datum_deel_1a: column.text({ optional: true }),
		datum_deel_1b: column.text({ optional: true }),
		doorvoercapaciteit: column.text({ optional: true }),
		doorvoercapaciteit_eenheid: column.text({ optional: true }),
		emissieklasse: column.text({ optional: true }),
		exportprijs_bedrag: column.text({ optional: true }),
		exportprijs_btw: column.text({ optional: true }),
		exportprijs_btw_percentage: column.text({ optional: true }),
		exportprijs_munteenheid: column.text({ optional: true }),
		extra01_naam: column.text({ optional: true }),
		extra01_waarde: column.text({ optional: true }),
		extra02_naam: column.text({ optional: true }),
		extra03_naam: column.text({ optional: true }),
		extra04_naam: column.text({ optional: true }),
		extra05_naam: column.text({ optional: true }),
		extra05_waarde: column.text({ optional: true }),
		extra06_naam: column.text({ optional: true }),
		extra06_waarde: column.text({ optional: true }),
		fijnstof_uitstoot: column.text({ optional: true }),
		fiscale_waarde: column.text({ optional: true }),
		garantielabel1_maanden: column.text({ optional: true }),
		garantielabel1_naam: column.text({ optional: true }),
		garantielabel1_type: column.text({ optional: true }),
		garantielabel2_maanden: column.text({ optional: true }),
		garantielabel2_naam: column.text({ optional: true }),
		garantielabel2_type: column.text({ optional: true }),
		garantielabel3_maanden: column.text({ optional: true }),
		garantielabel3_naam: column.text({ optional: true }),
		garantielabel3_type: column.text({ optional: true }),
		gemiddeld_verbruik: column.text({ optional: true }),
		generatorvermogen: column.text({ optional: true }),
		generatorvermogen_eenheid: column.text({ optional: true }),
		gvw: column.text({ optional: true }),
		highlights: column.text({ optional: true }),
		highlights_engels: column.text({ optional: true }),
		interieurkleur: column.text({ optional: true }),
		kenteken: column.text({ optional: true }),
		klantnummer: column.text({ optional: true }),
		klassieker: column.text({ optional: true }),
		kleur: column.text({ optional: true }),
		koppel: column.text({ optional: true }),
		kosten_rijklaar: column.text({ optional: true }),
		kraan: column.text({ optional: true }),
		laadklep: column.text({ optional: true }),
		laadschopinhoud: column.text({ optional: true }),
		laadschopinhoud_eenheid: column.text({ optional: true }),
		laadvermogen: column.text({ optional: true }),
		laktint: column.text({ optional: true }),
		land: column.text({ optional: true }),
		massa: column.text({ optional: true }),
		mastlengte: column.text({ optional: true }),
		mastlengte_eenheid: column.text({ optional: true }),
		max_trekgewicht_ongeremd: column.text({ optional: true }),
		maximale_koeltemperatuur: column.text({ optional: true }),
		maximale_koeltemperatuur_eenheid: column.text({ optional: true }),
		maximale_reikwijdte: column.text({ optional: true }),
		maximale_reikwijdte_eenheid: column.text({ optional: true }),
		meeneemprijs_bedrag: column.text({ optional: true }),
		meeneemprijs_bpm: column.text({ optional: true }),
		meeneemprijs_btw: column.text({ optional: true }),
		meeneemprijs_btw_percentage: column.text({ optional: true }),
		meeneemprijs_munteenheid: column.text({ optional: true }),
		meldcode: column.text({ optional: true }),
		merk: column.text({ optional: true }),
		minimale_koeltemperatuur: column.text({ optional: true }),
		minimale_koeltemperatuur_eenheid: column.text({ optional: true }),
		model: column.text({ optional: true }),
		modeldatum_tot: column.text({ optional: true }),
		modeldatum_vanaf: column.text({ optional: true }),
		nap_weblabel: column.text({ optional: true }),
		netto_catalogusprijs: column.text({ optional: true }),
		nieuw: column.text({ optional: true }),
		nieuwprijs: column.text({ optional: true }),
		oldtimer: column.text({ optional: true }),
		onderhoudsboekjes: column.text({ optional: true }),
		opbouw_hoogte: column.text({ optional: true }),
		opmerkingen: column.text({ optional: true }),
		opmerkingen_engels: column.text({ optional: true }),
		opmerkingen_garantie: column.text({ optional: true }),
		opmerkingen_handel: column.text({ optional: true }),
		opmerkingen_handel_engels: column.text({ optional: true }),
		prijs_nieuw_herberekend: column.text({ optional: true }),
		prijstype: column.text({ optional: true }),
		rupsbandbreedte: column.text({ optional: true }),
		rupsbandbreedte_eenheid: column.text({ optional: true }),
		staat_interieur: column.text({ optional: true }),
		staat_optisch: column.text({ optional: true }),
		standaardopmerkingen: column.text({ optional: true }),
		standaardopmerkingen_engels: column.text({ optional: true }),
		tellerstand: column.text({ optional: true }),
		tellerstand_eenheid: column.text({ optional: true }),
		titel: column.text({ optional: true }),
		titel_engels: column.text({ optional: true }),
		toepassingsgebied: column.text({ optional: true }),
		toepassingsmateriaal: column.text({ optional: true }),
		transmissie: column.text({ optional: true }),
		type: column.text({ optional: true }),
		verbruik_snelweg: column.text({ optional: true }),
		verbruik_stad: column.text({ optional: true }),
		verhoogd: column.text({ optional: true }),
		verkocht: column.text({ optional: true }),
		verkocht_datum: column.text({ optional: true }),
		verkoopprijs_handel_bedrag: column.text({ optional: true }),
		verkoopprijs_handel_bpm: column.text({ optional: true }),
		verkoopprijs_handel_btw: column.text({ optional: true }),
		verkoopprijs_handel_btw_percentage: column.text({ optional: true }),
		verkoopprijs_handel_munteenheid: column.text({ optional: true }),
		verkoopprijs_particulier_bedrag: column.text({ optional: true }),
		verkoopprijs_particulier_bpm: column.text({ optional: true }),
		verkoopprijs_particulier_btw: column.text({ optional: true }),
		verkoopprijs_particulier_btw_percentage: column.text({ optional: true }),
		verkoopprijs_particulier_munteenheid: column.text({ optional: true }),
		verlengd: column.text({ optional: true }),
		vermogen_brandstof_kw: column.text({ optional: true }),
		vermogen_brandstof_pk: column.text({ optional: true }),
		vermogen_motor_kw: column.text({ optional: true }),
		vermogen_motor_pk: column.text({ optional: true }),
		verplaatsing: column.text({ optional: true }),
		verwacht: column.text({ optional: true }),
		voedingsspanning: column.text({ optional: true }),
		voedingsspanning_eenheid: column.text({ optional: true }),
		voertuignr: column.text({ optional: true }),
		voertuignr_hexon: column.text({ primaryKey: true }),
		voertuignr_klant: column.text({ optional: true }),
		voertuigsoort: column.text({ optional: true }),
		wegenbelasting_kwartaal_max: column.text({ optional: true }),
		wegenbelasting_kwartaal_min: column.text({ optional: true }),
		wielbasis: column.text({ optional: true }),
		zoektermen: column.text({ optional: true }),
		zoektermen_engels: column.text({ optional: true }),
	},
});

// https://astro.build/db/config
export default defineDb({
	tables: { Voorraad },
});
